
const items = Array.from({ length: 47 }, (_, i) => `Item ${i + 1}`)
const ITEMS_PER_PAGE = 10

function App() {

  return (
    <div className="app">
      <h1>Pagination</h1>
    </div>
  )
}

export default App
